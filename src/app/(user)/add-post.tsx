import { View, Text } from 'react-native';
import React from 'react';

const addPost = () => {
    return (
        <View>
            <Text>Add Post</Text>
        </View>
    );
};

export default addPost;
